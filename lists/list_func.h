#include "listNode.h"
#include <iostream>

/// Utility to trace and print a list from head to tail
void print (listNode * n); 
/// Utilitty to do inplace reversal of a linked list
listNode * reverse (listNode * n); 
/// Utilitty to do create a copy of a given list
listNode * copy (listNode * n);
